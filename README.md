OpenCV
======
iOS framework built with contrib features.

Requirement
-----------
CMake
XCode

Build instruction
-----------------
Run `build.sh`. The output framework (`opencv2.framework.zip`) is stored at the root directory of repo.
